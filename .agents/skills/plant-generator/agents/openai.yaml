interface:
  display_name: "Plant Generator"
  short_description: "Repo-specific patterns and workflows for plant-generator"
  default_prompt: "Use the plant-generator repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true
# Preset gallery

Generates eight curated presets with fixed seeds, iteration counts, and bounded
topology. Render the exported results in a spaced grid for a stable visual
catalog.

See the [preset gallery guide](../../docs/examples/preset-gallery.md).

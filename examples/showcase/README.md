# Rojo showcase

Compile examples, then serve this project with Rojo. Studio attributes on
`Workspace.PlantGeneratorControls` control seed, growth, LOD, and regeneration;
the folder also reports symbol/segment/vertex statistics. Output is cleaned
before regeneration and uses only generated Parts.
